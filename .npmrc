# @vercel/analytics declares optional framework peers (Svelte/Vue/etc.) that npm 11 tries to
# resolve, dragging in a vite@8 beta that clashes with vitest's vite@5. The package works fine on
# Next 15 / React 19 — relax optional-peer resolution so local AND Vercel installs succeed.
legacy-peer-deps=true
